import { ResumePage } from '@/features/content/resume';

export const metadata = { title: 'Resume - Admin', description: 'Build and manage your resume' };

export default function AdminResumePage() { return <ResumePage />; }
