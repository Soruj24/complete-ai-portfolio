import { ContactRequestsPage } from '@/features/communication/contact-requests';

export const metadata = { title: 'Contact Requests - Admin', description: 'Manage incoming inquiries' };

export default function AdminContactRequestsPage() { return <ContactRequestsPage />; }
